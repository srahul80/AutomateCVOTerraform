# AutomateCVOTerraform

These terraform templates allows you to deploy your Cloud Manager(Connector Instance) and Cloud Volumes ONTAP instance in AWS.

Install Terraform and hyperscaler CLI as a pre-requisties 
